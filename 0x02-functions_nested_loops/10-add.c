#include "main.h"
/**
 * add - adds two integers and returns the result
 * @num1: number to be computed
 * @num2: number to be added
 * Return: always 0
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
