#include "main.h"
/**
 * mul -  multiply two integers
 * @a: integer
 * @b: integer to be multiplied
 * @c: product
 * Return: always 0
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
