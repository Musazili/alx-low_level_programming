#include "main.h"
/**
 * _strcpy - entry point to copy
 * @dest: destination
 * @src: source
 */

char *_strcpy(char *dest, char *src)
{

	int i;

	for (i = 0; src[i] != '\0'; i++)
	{
		dest[i] = src[i];
	}
	dest[i] = src[i];
	return (&dest[0]);
}
